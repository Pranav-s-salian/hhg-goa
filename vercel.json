{
  "buildCommand": "cd frontend && cp package.vercel.json package.json && cd ../shared && npm install && cd ../frontend && npm install && npm run build",
  "installCommand": "echo 'Using custom build command'"
}
